rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes pass through authenticated server Route Handlers.
    // The Firebase Admin SDK is authorized through IAM and bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
